package InterfaceClass;

// 'implements' is used as 'extends' in interface function.
// we can add many interface using using comma example is below.
// we can also add class using extend but we have to put before implements.
// that means extends comes first then implements.
// we can use as many implements as we can using comma.


public class interfaceImplements extends demo3 implements demo1,demo2{

	
	@Override
	public void m4() {
		// TODO Auto-generated method stub
		
	}

	@Override
	public void m5() {
		// TODO Auto-generated method stub
		
	}

}
