package InterfaceClass;

//we can also use 'extends' to call any other interface but not class. 
// since demo3 is class function we cannot extend it.
// we can extend as many interface as we can like implement.

public interface demo4 extends demo1,demo2 {//demo3  

}
